## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}

The frontend is designed as a forced "dark mode" professional portfolio to match the AI video creator aesthetic.
Smooth scrolling is implemented natively via CSS.
